# 飞书凭证（绝不进仓库）
.env
.env.local

# 依赖与部署产物
node_modules/
.vercel/
vercel-latest.lock

# 系统 / 临时文件
.DS_Store
Thumbs.db
*.tmp
*.log

# 本地化工作台私有 DeepSeek Key（本地私有，绝不进仓库）
sem_key_local.txt

# 团队离线分发包（235MB exe + 展开目录，仅本地保留，不进仓库）
dist/

# 注意：v4/data/ 是同步产出的静态数据，必须提交，
# 否则 GitHub Pages 上的工作台读不到飞书表格。

# v4 本地验证截图（不入仓，E2E 临时产物）
v4/_ev_*.png
v4/_v4local_shot_*.png
v4/_*_shot*.png

# v4 本地 E2E 生成的临时 SRT 输入（不入仓）
v4/_*_原文.srt

# v4.11 重构备份与验证产物（不入仓）
v4/backup-before-*/
review-batch.png
test-results.json
